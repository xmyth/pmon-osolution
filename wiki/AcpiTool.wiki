#summary acpi dump tool in linux

  * acpidump utilities to dump system's ACPI tables to an ASCII file
{{{
      $sudo apt-get install acpidump
}}}

  * iasl ntel ASL compiler/decompiler
{{{
      $sudo apt-get install iasl
}}}

